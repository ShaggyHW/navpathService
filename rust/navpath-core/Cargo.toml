[package]
name = "navpath-core"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Core pathfinding library for navpath in Rust."

[lib]
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
rusqlite = { workspace = true }
lru = { workspace = true }
tracing = { workspace = true }
